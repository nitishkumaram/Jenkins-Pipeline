pipeline {
    agent any
    stages{
        stage('Retry'){
            steps{
                retry(3){
                    echo "before throwing error"
                    error "error in retry"
                }
                echo "after retry(3)"
            }
        }

        stage()
    }
}